import { X } from 'lucide-react';
import type { ContourStageManifest } from '../api/contourExtractorApi';

export const ContourStageGuideModal=({stage,onClose}:{stage:ContourStageManifest;onClose:()=>void})=><div className="fixed inset-0 z-[190] flex items-center justify-center bg-black/75 p-4"><div className="w-full max-w-3xl rounded-xl border border-[#5f6368] bg-[#202124] shadow-2xl"><header className="flex items-center gap-2 border-b border-[#3c4043] bg-[#292a2d] px-4 py-3"><div><div className="text-[8px] font-black uppercase tracking-[.18em] text-[#81c995]">Contour stage guide</div><div className="text-sm font-black">{stage.label}</div></div><button onClick={onClose} className="ml-auto"><X size={16}/></button></header><div className="grid grid-cols-[1fr_300px] gap-4 p-4"><div><div className="text-[10px] font-black text-[#8ab4f8]">QUESTION THIS STAGE ANSWERS</div><p className="mt-2 text-[11px] leading-5 text-[#e8eaed]">{stage.guide?.question??stage.description}</p><div className="mt-5 text-[10px] font-black text-[#9aa0a6]">WHY IT EXISTS</div><p className="mt-2 text-[10px] leading-5 text-[#bdc1c6]">{stage.description}</p>{stage.guide?.notes?.length?<ul className="mt-4 space-y-2 text-[10px] text-[#9aa0a6]">{stage.guide.notes.map((n:string)=><li key={n}>• {n}</li>)}</ul>:null}</div><div className="rounded border border-[#3c4043] bg-[#292a2d] p-3"><div className="text-[9px] font-black text-[#fdd663]">PARAMETERS</div>{Object.entries(stage.parameters).map(([name,spec])=><div key={name} className="mt-3 border-t border-[#3c4043] pt-2"><div className="text-[9px] font-black">{spec.label??name}</div><div className="mt-1 font-mono text-[8px] text-[#8ab4f8]">default = {String(spec.default)}</div>{spec.description?<div className="mt-1 text-[8px] leading-4 text-[#9aa0a6]">{spec.description}</div>:null}</div>)}</div></div></div></div>;
